<% if @users.blank? %>
<p>There are not any users currently in the system.</p>
<% else %>
<p>These are the current users in our system</p>

<ul id = "users">
    <% @users.each do |c| %>
        <li>
            <%= c.id %> <%= c.role.title %>
        </li>
    <% end %>
</ul>
<% end %>